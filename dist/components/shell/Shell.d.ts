import React, { ReactNode } from 'react';
import './Shell.css';
interface ShellProperties {
    children: ReactNode;
}
export declare const Shell: ({ children }: ShellProperties) => React.JSX.Element;
export {};
