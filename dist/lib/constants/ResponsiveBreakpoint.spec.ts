import { ResponsiveBreakpoints } from './ResponsiveBreakpoint'

describe('ResponsiveBreakpoint', () => {

  // #region ResponsiveBreakpoints
  describe('ResponsiveBreakpoints', () => {

    test('S breakpoint equals 769', () => {
      // Declaration
      // Execution
      // Assertions
      expect(ResponsiveBreakpoints.S).toBe(769)
    })

    test('ResponsiveBreakpoints is an object', () => {
      // Declaration
      // Execution
      // Assertions
      expect(typeof ResponsiveBreakpoints).toBe('object')
    })

  })
  // #endregion

})
